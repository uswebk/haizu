VITE_API_URL=http://localhost:3001
VITE_APP_ENV=local

# Default deployment language (en | ja; defaults to en). Users can switch this via the screen.
VITE_DEFAULT_LOCALE=en

# Default deployment time zone (IANA name; falls back to the runtime time zone if unset)
VITE_DEFAULT_TIMEZONE=UTC
